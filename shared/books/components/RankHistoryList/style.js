// @flow
// NPM dependencies
import { StyleSheet } from 'react-native';

// App dependencies
import { GREY_LIGHTER } from 'shared/style/color';

export default StyleSheet.create({
  root: {
    flex: 1,
  },
  separator: {
    backgroundColor: GREY_LIGHTER,
    height: 1,
  },
});
